language: objective-c
osx_image: xcode8
env:
  matrix:
    - DESTINATION="platform=iOS Simulator,OS=10.0,name=iPad Pro (12.9 inch)"

script: xcodebuild test -project Pitch.xcodeproj -scheme Pitch -destination "${DESTINATION}" TOOLCHAINS=swift
